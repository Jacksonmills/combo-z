import styled from 'styled-components';

const ComboSpacer = () => {
  return <Wrapper>&gt;</Wrapper>;
};

const Wrapper = styled.span`
  font-size: ${35 / 16}rem;
  font-weight: 900;
`;

export default ComboSpacer;
